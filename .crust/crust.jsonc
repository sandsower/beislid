{
  // Crust project manifest for this repo, generated alongside `crust import
  // beislid-workflow --write`. The importer drafts module files only; the
  // manifest itself is hand-authored per BEI-137 (crust has no `init`
  // subcommand yet). Profile "portable" requires gates + policy; this repo's
  // .beislid/workflow.md also yields workflow and integrations modules, which
  // portable accepts as additive extras.
  "version": "crust.project/v1",
  "project": { "name": "beislid" },
  "profile": "portable"
}
